{
  "scripts": {
    "build:pack": "node ./utils/pack.mjs",
    "build:unpack": "node ./utils/unpack.mjs",
    "build:fix-names": "node ./utils/fix-names.mjs"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^1.0"
  }
}
